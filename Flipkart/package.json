{
  "name": "flipkart-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Modular ecommerce API - products / cart / wishlist (TypeScript + Express 5 + Mongoose)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsup --minify --watch --onSuccess \"node dist/server.js\"",
    "build": "tsup",
    "build:prod": "tsup --minify --clean",
    "start:prod": "pnpm build:prod && NODE_ENV=production node dist/server.js",
    "seed:products": "tsup && node dist/seed-products.js",
    "seed:test": "tsup && NODE_ENV=test node dist/seed-products.js",
    "start:test": "pnpm build:prod && NODE_ENV=test node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "compression": "^1.8.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.5",
    "negotiator": "^1.1.0",
    "redis": "^6.2.1",
    "swagger-jsdoc": "^6.3.0",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.19.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/compression": "^1.8.1",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/negotiator": "^0.6.5",
    "@types/node": "^26.4.1",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}
